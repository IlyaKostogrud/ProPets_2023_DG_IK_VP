import React from 'react';

const LoginForm = () => {
    return (
        <div>
            Login form
            <button>Cancel</button>
            <button>Submit</button>
        </div>
    );
};

export default LoginForm;